{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/reservation.schema.json",
  "title": "Reservation",
  "description": "A Reservation created by a user",
  "type": "object",
  "properties": {
    "reservation_id": {
      "description": "The unique id given to the reservation",
      "type": "integer"
    },
    "created_by": {
      "description": "The user that created the reservation",
      "type": "string"
    },
    "start_date": {
      "description": "The start date of the reservation, will be a stringified Date object",
      "type": "string",
      "format": "date-time"
    },
    "start_date_epoch": {
      "description": "The start date of the reservation as a epoch in seconds",
      "type": "integer"
    },
    "end_date": {
      "description": "The end date of the reservation, will be a stringified Date object",
      "type": "string",
      "format": "date-time"
    },
    "end_date_epoch": {
      "description": "The end date of the reservation as an epoch in seconds",
      "type": "integer"
    },
    "reservation_type": {
      "description": "Type of reservation, currently open or closed, but leaving as string in case new types arise",
      "type": "string",
      "enum": ["open", "closed"],
      "default": "closed"
    }
  },
  "required": [
    "reservation_id",
    "created_by",
    "start_date",
    "start_date_epoch",
    "end_date",
    "end_date_epoch",
    "reservation_type"
  ]
}